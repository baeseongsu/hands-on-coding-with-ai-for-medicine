# 이 저장소의 모든 프로젝트가 공유하는 Python 패키지.
#
# 의도적으로 비어 있습니다. 프로젝트 01은 Node 툴체인으로 애플리케이션을 만들고,
# 데이터 전처리 스크립트의 언어는 수강생이 선택하므로, 모든 프로젝트가 동시에
# 필요로 하는 패키지가 현재는 없습니다. 패키지가 없어도 가상환경 자체는 의미가
# 있습니다. 인터프리터 버전을 고정하고, 수강생이 시스템 Python에 직접 설치하는
# 것을 막습니다.
#
# 특정 프로젝트에만 필요한 패키지는 아래 위치에 선언하세요.
#   projects/<세션>-<프로젝트>/requirements.txt
# devcontainer가 onCreateCommand 단계에서 그런 파일을 모두 찾아 같은 가상환경에
# 설치합니다. 이 파일이나 devcontainer.json 을 수정할 필요는 없습니다.
#
# 모든 프로젝트가 정말로 필요로 할 때만 여기에 추가하세요.
