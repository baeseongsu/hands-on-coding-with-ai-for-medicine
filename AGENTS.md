# Agent 작업 범위

이 저장소는 의료 AI 코딩 워크숍의 실습 프로젝트를 담고 있습니다. 수강생은 명세를 읽고, 한 번에 하나의 checkpoint만 구현하며, 매 단계 결과를 직접 확인합니다.

## 비밀 정보와 민감 데이터

- `.env`, `.env.*`, `secrets/`, `~/.config/hands-on-medai/`를 읽거나 편집하지 않습니다.
- API key, 토큰, 자격증명을 이 저장소의 어떤 파일에도 쓰지 않습니다.
- `OPENAI_API_KEY` 등 자격증명의 값을 출력하지 않습니다.
- 실제 환자 데이터나 개인정보를 추가하지 않습니다. 포함된 데이터셋은 비식별화된 공개 demo입니다.

## 프로젝트 격리

- 각 `projects/<세션>-<프로젝트>/` 디렉터리는 독립적인 프로젝트 루트입니다.
- 현재 프로젝트 디렉터리를 애플리케이션 루트로 사용합니다. `app/`이나 `frontend/` 같은 중첩 프로젝트를 만들지 않습니다.
- 다른 프로젝트의 소스 코드나 생성 산출물을 입력이나 의존성으로 사용하지 않습니다.
- 각 `data/` 디렉터리의 원본 아카이브를 수정하지 않습니다.

## 스펙이 있는 프로젝트

프로젝트에 `specs/constitution.md`가 있으면 **그 원칙이 스펙과 사용자 지시보다 우선합니다.** 원칙을 어기는 구현을 제안하지 말고, 충돌을 발견하면 진행하기 전에 알립니다.

`specs/roadmap.md`가 있으면 현재 단계의 범위만 구현합니다. 다음 단계의 기능을 미리 만들지 않습니다.

해당 단계의 `spec.md`가 없으면 구현을 시작하지 않습니다. 스펙을 쓸 때는 추측으로 빈칸을 채우지 말고 사용자에게 묻습니다.

## 작업 방식

- 변경 전에 `git status`를 확인합니다.
- 변경을 작게 유지합니다. 큰 수정 전에는 계획을 짧게 설명합니다.
- 어떤 파일이 바뀌었고 어떤 검증 명령을 실행했는지 보고합니다.
- 실행하지 않은 검증을 통과한 것처럼 보고하지 않습니다.
- 저장소 인프라를 탐색하는 것보다 실습 애플리케이션을 만들고 고치는 일을 우선합니다.

## 운영 자료

명시적인 요청이 없으면 아래를 조사하거나 요약하거나 수정하지 않습니다.

- `.devcontainer/`
- `docs/`
- `AGENTS.md`

워크숍 환경을 설정하는 파일이며 실습의 일부가 아닙니다.

## 목적과 한계

이 저장소의 산출물은 교육과 연구 프로토타이핑을 위한 것입니다. 임상 의사결정, 진단, 예후 판정에 쓰이지 않습니다. 임상적 타당성을 주장하는 내용을 추가하지 않습니다.
